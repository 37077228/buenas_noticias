<%= form_with(model: news) do |form| %>
  <% if news.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
        <% news.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container" >
    <%= form.label :header, style: "display: block" %>
    <%= form.text_field :header %>
  </div>

  <div class="container">
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div class="container">
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>

  <div class="container">
    <%= form.label :types, style: "display: block" %>
    <%= form.select :types, [1, 2, 3, 4 ,5], include_blank: true %> 
  </div>

  <div class="container">
    <%= form.submit %>
  </div>
<% end %>
